name: Deployment

on:
  push:
    branches:
      - checker

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: https://github.com
    steps:
      - name: deploy
        run: |
          echo "Deploying to staging"
